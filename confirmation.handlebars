<h1>Your appointment has been scheduled!</h1>
<section id='details'>
    <div>{{day_of_week}}, {{month}} {{day}}, {{year}} at {{time}}</div>
    <div>{{patient}} and {{doctor}}</div>
    <div>{{location}}</div>
</section>
<section id='actions'>
    <a href='/schedule'>Schedule Another Appointment</a>
    <a href={{lookup}}>View Patient Details</a>
    <a href='/'>Home</a>
</section>